require("lualine").setup({
    options = {
        icons_enabled = true,
        globalstatus = true,
        theme = "rose-pine",
        -- section_separators = "",
        -- component_separators = "",
    },
})
